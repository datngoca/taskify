import classNames from "classnames/bind";
import React, { useState } from "react";

import styles from "./TaskInput.module.css";

const cx = classNames.bind(styles);

const TaskList = () => {
  return (
    <>
      <h1>Đây là tính năng TaskList</h1>
    </>
  );
};

export default TaskList;
