import classNames from "classnames/bind";
import styles from "./TaskInput.module.css";

const cx = classNames.bind(styles);

const TaskInput = () => {
  return (
    <>
      <h1>Đây là tính năng taskInput</h1>
    </>
  );
};

export default TaskInput;
